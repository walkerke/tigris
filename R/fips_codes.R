#' Dataset with FIPS codes for US states and counties
#'
#' Built-in dataset for use with the \code{lookup_code} function.  To access the data directly, issue the command \code{load(fips_codes)}.

"fips_codes"
